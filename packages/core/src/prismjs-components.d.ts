// Prism grammar component files ship without type declarations. They are imported
// only for their side effect of registering a language on the shared Prism instance.
declare module 'prismjs/components/*';
